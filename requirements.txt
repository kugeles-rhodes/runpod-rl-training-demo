stable-baselines3==2.9.0
gymnasium[box2d,classic-control]==1.3.0
tensorboard==2.21.0
# torch is pulled in by stable-baselines3 (requires >=2.8,<3). Left unpinned so
# the CUDA build preinstalled on RunPod PyTorch images is reused, not replaced.
